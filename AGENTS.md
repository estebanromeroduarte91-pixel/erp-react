# Preferencia del usuario para este proyecto

Cuando el usuario deba ejecutar SQL manualmente en Supabase, pega el SQL completo en el chat dentro de un bloque `sql`; no entregues solo una ruta o enlace al archivo. Si hay varios scripts, indica el orden de ejecución. No incluyas secretos ni credenciales en el SQL compartido.
